# scripts/vocabulary

Scripts for working with the Anthropic API.

## test-anthropic.mts

Sends a single request to Claude to verify that the API key and connectivity are working.

```bash
node --experimental-strip-types --env-file=.env scripts/vocabulary/test-anthropic.mts
```
